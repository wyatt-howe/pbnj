const mathjs = require('mathjs');

function parse(definitions, context) {
  if (context == null) {
    context = {};
  }

  /*
  Expand context
  */

  return context;
}

function evaluate(expression, context) {
  if (context == null) {
    context = {};
  }

  let value;
  /*
  Evaluate expression in context
  */

  return value;
}

module.exports = {
  parse: parse,
  evaluate: evaluate
}
